{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "python-resume.fullname" . }}
  labels:
    {{- include "python-resume.labels" . | nindent 4 }}
type: Opaque
data:
  {{ .Values.existingSecretKey }}: {{ .Values.resumeUpdateSecret | b64enc }}
{{- end }}
