# Values for running in kind (NodePort, optional local images)
service:
  type: NodePort
  # nodePort: 30080  # Uncomment to pin NodePort

# If using locally built images and loading into kind:
# image:
#   resume:
#     pullPolicy: Never
#   pandoc:
#     pullPolicy: Never
